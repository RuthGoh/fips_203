// bit vec

fn bits_to_bytes(bit_vec:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}

fn bytes_to_bits(byte_vec:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}

fn compress(mod_exp:u8, comp_vec:Vec<u16>) -> Vec<u16> {
    return Vec::new();
}

fn decompress(mod_exp:u8, decomp_vec:Vec<u16>) -> Vec<u16> {
    return Vec::new();
}

fn byte_encode(mod_exp:u8, int_vec:Vec<u16>) -> Vec<u8> {
    return Vec::new();
}

fn byte_decode(mod_exp:u8, byte_vec:Vec<u8>) -> Vec<u16> {
    return Vec::new();
}