fn keygen_internal(rand_d:Vec<u8>, rand_z:Vec<u8>) -> (Vec<u8>,Vec<u8>) {
    return (Vec::new(),Vec::new());
}

fn encaps_internal(encaps_k:Vec<u8>, rand:Vec<u8>) -> (Vec<u8>,Vec<u8>) {
    return (Vec::new(),Vec::new());
}

fn decaps_internal(decaps_k:Vec<u8>, ctext:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}

fn keygen() -> (Vec<u8>,Vec<u8>) {
    return (Vec::new(),Vec::new());
}

// 'checked input'
fn encaps(encaps_k:Vec<u8>) -> (Vec<u8>,Vec<u8>) {
    return (Vec::new(),Vec::new());
}

fn decaps(decaps_k:Vec<u8>, ctext:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}