fn keygen(rand:Vec<u8>) -> (Vec<u8>,Vec<u8>) {
    return (Vec::new(), Vec::new());
}

fn encrypt(enc_k:Vec<u8>, message:Vec<u8>, rand:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}

fn decrypt(dec_k:Vec<u8>, ctext:Vec<u8>) -> Vec<u8> {
    return Vec::new();
}